import { styled } from 'styled-components';

export const ToggleIcon = styled.svg`
  width: 14px;
  height: 14px;
  overflow: visible;
  fill: none;
  stroke: currentColor;
  stroke-linecap: round;
  stroke-width: 1.8;
`;
